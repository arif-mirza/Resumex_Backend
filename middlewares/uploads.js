// import multer from "multer";

// // Memory storage (store file in memory buffer, not disk)
// const storage = multer.memoryStorage();
// const upload = multer({ storage });

// export default upload;
